---
	- name: installing nginx server
		hosts: all
		become: yes
		tasks: 
		- name: Installing nginx using yum module
			ansible.builtin.yum:
				name: nginx
				state: installed
		- name: Checking the status
			ansible.builtin.service:
				name: nginx
				state: started
