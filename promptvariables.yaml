- name: Prompt Variables
  hosts: localhost
  become: yes
  vars_prompt:
    name: USERNAME
    prompt: Give me user name
    private: false
    name: PASSWORD
    prompt: Give me PASSWORD
    private: false
  tasks:
  - name: Using Prompt Variables
    debug:
      msg: "I am Giving {{USERNAME}}, I am giving password {{PASSWORD}}"
